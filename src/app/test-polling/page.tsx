import PollingTest from '@/components/PollingTest';

export default function TestPollingPage() {
  return (
    <div style={{ padding: 20 }}>
      <h1 style={{ textAlign: 'center', marginBottom: 20 }}>
        🧪 Test du Système de Polling
      </h1>
      <PollingTest />
    </div>
  );
} 